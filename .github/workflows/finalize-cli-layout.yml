name: Finalize CLI layout

on:
  pull_request:
    branches:
      - main

permissions:
  contents: write

jobs:
  finalize:
    if: github.actor != 'github-actions[bot]'
    runs-on: ubuntu-latest
    steps:
      - name: Check out branch
        uses: actions/checkout@v4
        with:
          ref: agent/standardize-cli-layout
          fetch-depth: 0

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: 1.3.13

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Apply repository fixers
        run: |
          bun run lint:fix
          bun run format

      - name: Validate
        run: |
          bun run knip
          bun run typecheck
          bun run build
          bun run test
          bun run docs
          bun dist/cli/standalone.js validate .

      - name: Remove one-time workflow
        run: rm .github/workflows/finalize-cli-layout.yml

      - name: Commit finalized migration
        run: |
          git config user.name github-actions[bot]
          git config user.email 41898282+github-actions[bot]@users.noreply.github.com
          git add -A
          git commit -m "Finalize canonical CLI layout"
          git push origin HEAD:agent/standardize-cli-layout
