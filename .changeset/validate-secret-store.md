---
'@ankhorage/doctor': minor
---

Validate canonical secret-store selection and OAuth credential references, reject inline secret values, duplicate provider IDs, invalid callback routes, and enabled OAuth providers without a configured secret store.
