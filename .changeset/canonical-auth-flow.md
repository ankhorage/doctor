---
'@ankhorage/doctor': minor
---

Validate app manifest JSON files against the canonical `infra.auth.flow` contract, reject removed `settings.authFlow` configuration with a manual-migration diagnostic, and validate authorization only when it is explicitly configured.
